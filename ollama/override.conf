#/etc/systemd/system/ollama.service.d/override.conf
[Service]
Environment="OLLAMA_ORIGINS=*"
Environment="OLLAMA_HOST=0.0.0.0:11434"
#customization 12gb vram 32gb ram free memory
# Weights stay in VRAM (fast); Ollama auto-offloads overflow layers (KV + weights)
# to RAM when the 64k context exceeds 12GB VRAM, extending context without touching
# normal-size sessions. Flash attention + q8 KV shrink KV cache so more stays in VRAM.
Environment="OLLAMA_FLASH_ATTENTION=1"
Environment="OLLAMA_KV_CACHE_TYPE=q8_0"
Environment="OLLAMA_CONTEXT_LENGTH=65536"
# Added 2026-08-08 (verified against `ollama serve --help` + envconfig/config.go v0.32.5):
# KEEP_ALIVE=-1 keeps loaded models hot between prompts (kills 5-15s reloads in opencode);
# MAX_LOADED_MODELS=2 pins main (deepseek-r1:8b) + small (qwen3.5:4b) both resident;
# GPU_OVERHEAD=1GiB reserves VRAM headroom so deep 64k sessions don't CUDA-OOM on 12GB.
Environment="OLLAMA_KEEP_ALIVE=-1"
Environment="OLLAMA_MAX_LOADED_MODELS=2"
Environment="OLLAMA_GPU_OVERHEAD=1073741824"